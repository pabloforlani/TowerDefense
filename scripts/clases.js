import * as index from "./principalFunctions.js";

class Objeto {
    constructor(x, y, ancho, alto, color){
        this.x = x;
        this.y = y;
        this.ancho = ancho;
        this.alto = alto;
        this.color = color;
    }

    dibujar = function(){
        
    }
}